num = int(input("Ingrese un Numero:"))  			#Pido al usuario que ingrese un numero para evaluar paridad
if num % 2 == 0:									#Evaluo el numero si es Par, haciendo la division pero tomando su resto
	print "El Numero",num, "ingresado Es **Par**"	#Muestro por pantalla si es Par

